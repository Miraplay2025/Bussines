services:
  - type: web
    name: wppconnect-flask
    env: python
    region: oregon
    plan: free
    buildCommand: |
      npm install
      pip install -r requirements.txt
    startCommand: python3 app.py
    autoDeploy: true
    envVars:
      - key: PORT
        value: "5000"
